Simple Cloudflare worker script to enable maintenance mode for sites. Uses wrangler to enable/ disable

### To deploy after changes made:

Install wrangler:
`npm install -g wrangler`

Use requried node verison:
`nvm use`

Wrangler login:
`wrangler login`

Deploy:
`wrangler deploy`
